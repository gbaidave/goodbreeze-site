import type { ReactNode } from '

export default function AuthLayout({ children }: { children: ReactNode }) {
  return (
    <div className='>
      <div className='>
        {/* Logo */}
        <div className='>
          <a href=' className='>
            <span className='>Good Breeze<span className='> AI</span></span>
          </a>
        </div>
        {children}
      </div>
    </div>
  )
}
